apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "zerotierrouter.fullname" . }}
  namespace: {{ .Release.Namespace }}
data:
  ZT_IFNAME: {{ .Values.config.internalInterfaceName }}
  ZT_ENABLE_FORWARD: {{ .Values.config.enableForwarding }}
  ZT_ENABLE_MASQUERADE: {{ .Values.config.enableMasquerade }}
